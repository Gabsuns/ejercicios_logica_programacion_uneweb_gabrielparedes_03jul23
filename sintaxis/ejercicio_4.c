#include <stdio.h>

int main() {
  // generacion de nuevas lineas

  /*
    Tambien puede generar varias lineas con una sola funcion "printf()". sin embargo, esto podria hcer que el codigo sea mas dificil de leer:
  */
  printf("Hola mundo\nEstoy aprendiendo leguaje C\nY es asombroso");
  
  /*
   podemos utilizar tantas funciones "printf()"
   como queramos. cin embargo tenemos que tener en cuenta que no se va a insertar una nueva linea al final de la salida.
  */
  return 0;
}